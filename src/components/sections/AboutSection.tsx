import { Flame, Heart, Leaf, Truck } from 'lucide-react';
import { restaurant } from '../../config/restaurant';
import { SectionLabel } from '../ui/SectionHeading';
const icons = { flame: Flame, leaf: Leaf, heart: Heart, truck: Truck };
export function AboutSection() { return <section id="about" className="bg-background py-20 sm:py-24 px-5"><div className="max-w-7xl mx-auto grid lg:grid-cols-2 gap-12 lg:gap-16 items-center">
  <div><SectionLabel>{restaurant.about.eyebrow}</SectionLabel><h2 className="font-display text-4xl sm:text-5xl font-black text-heading leading-tight">{restaurant.about.heading}<span className="block text-accent">{restaurant.about.highlightedHeading}</span></h2><p className="text-muted mt-6 leading-relaxed">{restaurant.about.description}</p><p className="text-muted mt-4 leading-relaxed">{restaurant.about.secondaryDescription}</p><div className="mt-9 grid sm:grid-cols-2 gap-5">{restaurant.about.pillars.map(pillar => { const Icon = icons[pillar.icon]; return <div key={pillar.title} className="flex gap-3"><span className="w-10 h-10 rounded-xl bg-primary/20 flex items-center justify-center shrink-0"><Icon size={18} className="text-heading"/></span><div><p className="text-heading font-bold text-sm">{pillar.title}</p><p className="text-muted text-xs mt-1 leading-relaxed">{pillar.description}</p></div></div>; })}</div></div>
  <div className="relative h-[400px] sm:h-[520px]"><img src={restaurant.about.primaryImage} alt={restaurant.about.primaryImageAlt} loading="lazy" className="absolute inset-y-0 left-0 w-[82%] h-full object-cover rounded-[2rem]"/><div className="absolute right-0 bottom-8 w-[42%] h-48 sm:h-64 rounded-2xl overflow-hidden border-4 border-background shadow-xl"><img src={restaurant.about.secondaryImage} alt={restaurant.about.secondaryImageAlt} loading="lazy" className="w-full h-full object-cover"/></div></div>
  </div></section>; }
