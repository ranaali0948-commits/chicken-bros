import { MessageCircle } from 'lucide-react';
import { restaurant } from '../../config/restaurant';
import { reviews } from '../../data/reviews';
import { SectionHeading } from '../ui/SectionHeading';
import { StarRating } from '../ui/StarRating';
export function ReviewsSection() { return <section id="reviews" className="bg-background-alt py-20 sm:py-24 px-5"><div className="max-w-7xl mx-auto"><div className="text-center mb-12"><SectionHeading eyebrow={restaurant.reviews.eyebrow} heading={restaurant.reviews.heading}/>{restaurant.reviews.summary && <p className="text-muted mt-3">{restaurant.reviews.summary}</p>}</div>{reviews.length ? <div className="grid md:grid-cols-3 gap-5">{reviews.map(review => <article key={review.id} className="bg-surface border border-border rounded-2xl p-7"><StarRating count={review.rating}/><p className="text-heading mt-4 leading-relaxed">“{review.text}”</p><p className="font-bold text-heading mt-5">{review.name}</p><p className="text-muted text-xs mt-1">{review.date}</p></article>)}</div> : <div className="max-w-2xl mx-auto bg-surface border border-border rounded-2xl p-8 text-center"><span className="w-14 h-14 bg-primary/20 rounded-full flex items-center justify-center mx-auto"><MessageCircle className="text-heading"/></span><p className="text-muted mt-5">{restaurant.reviews.emptyState}</p></div>}</div></section>; }
