import { restaurant } from '../../config/restaurant'; import { reviews } from '../../data/reviews'; import { useInView } from '../../hooks/useInView'; import { SectionHeading } from '../ui/SectionHeading'; import { StarRating } from '../ui/StarRating';
export function ReviewsSection(){const{ref,inView}=useInView();return <section id="reviews" className="bg-background-alt py-24 px-6"><div ref={ref} className="max-w-7xl mx-auto"><div className={`text-center mb-14 transition-all duration-700 ${inView?'opacity-100 translate-y-0':'opacity-0 translate-y-8'}`}><SectionHeading eyebrow={restaurant.reviews.eyebrow} heading={restaurant.reviews.heading}/><div className="flex items-center justify-center gap-2 mt-4"><StarRating count={5}/><span className="text-charcoal-300 text-sm">{restaurant.reviews.summary}</span></div></div><div className="grid md:grid-cols-3 gap-6">{reviews.map((review,i)=><article key={review.id} className={`bg-charcoal-800/60 border border-charcoal-700/60 rounded-2xl p-7 transition-all duration-700 hover:border-brand-500/30 ${inView?'opacity-100 translate-y-0':'opacity-0 translate-y-8'}`} style={{transitionDelay:`${i*100}ms`}}><StarRating count={review.rating}/><p className="text-charcoal-200 mt-4 leading-relaxed text-sm">&ldquo;{review.text}&rdquo;</p><div className="flex items-center justify-between mt-6 pt-5 border-t border-charcoal-700/60"><div className="flex items-center gap-3"><div className="w-9 h-9 rounded-full bg-brand-500/20 flex items-center justify-center text-brand-400 font-bold text-sm">{review.name[0]}</div><span className="text-white font-medium text-sm">{review.name}</span></div><span className="text-charcoal-500 text-xs">{review.date}</span></div></article>)}</div></div></section>}
