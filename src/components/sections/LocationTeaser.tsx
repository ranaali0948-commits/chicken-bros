import { ArrowRight, MapPin, Phone } from 'lucide-react';
import { restaurant } from '../../config/restaurant';
import { handleInternalNavigation } from '../../hooks/useRoute';

export function LocationTeaser() { return <section className="bg-surface px-5 py-20 sm:py-24"><div className="max-w-7xl mx-auto grid lg:grid-cols-[1fr_1.1fr] gap-10 items-end"><div><p className="eyebrow">Paris 18</p><h2 className="editorial-title">VOTRE PROCHAINE<br/><span className="text-accent">PAUSE GOURMANDE.</span></h2></div><div className="lg:border-l lg:border-border lg:pl-12"><p className="font-display text-2xl font-black text-heading">{restaurant.address}<br/>{restaurant.postcode} {restaurant.city}</p><p className="text-muted mt-4">{restaurant.openingHours[0].label} · {restaurant.openingHours[0].value}</p><div className="flex flex-wrap gap-3 mt-7"><a href={restaurant.googleMapsUrl} target="_blank" rel="noopener noreferrer" className="btn-primary"><MapPin size={17} className="mr-2"/>Itinéraire</a><a href={`tel:${restaurant.phone}`} className="btn-secondary"><Phone size={17} className="mr-2"/>Nous appeler</a><a href="/contact" onClick={event => handleInternalNavigation(event, '/contact')} className="text-link px-2">Voir l’adresse <ArrowRight size={17}/></a></div></div></div></section>; }
