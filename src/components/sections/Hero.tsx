import { ArrowRight, ChevronDown, Flame } from 'lucide-react';
import { restaurant } from '../../config/restaurant';
import { Button } from '../ui/Button';
export function Hero() { const scroll = (id: string) => document.getElementById(id)?.scrollIntoView({ behavior: 'smooth' }); return <section id="hero" className="relative min-h-screen flex flex-col items-center justify-center overflow-hidden"><div className="absolute inset-0"><img src={restaurant.hero.image} alt={restaurant.hero.imageAlt} className="w-full h-full object-cover object-center scale-105" style={{ filter: 'brightness(0.35)' }} /><div className="absolute inset-0 bg-gradient-to-b from-charcoal-900/60 via-charcoal-900/20 to-charcoal-900" /></div><div className="relative z-10 text-center px-6 max-w-4xl mx-auto"><div className="inline-flex items-center gap-2 bg-brand-500/15 border border-brand-500/30 text-brand-400 text-xs font-semibold tracking-widest uppercase px-4 py-2 rounded-full mb-8 animate-fade-in"><Flame size={12} className="fill-brand-500 text-brand-500" />{restaurant.hero.eyebrow}</div><h1 className="font-display text-5xl sm:text-7xl lg:text-8xl font-black text-white leading-[1.05] tracking-tight mb-6 animate-fade-up">{restaurant.hero.heading}<span className="block italic text-brand-500">{restaurant.hero.highlightedHeading}</span></h1><p className="text-charcoal-200 text-lg sm:text-xl font-light max-w-xl mx-auto leading-relaxed mb-10 animate-fade-up" style={{ animationDelay: '0.1s', opacity: 0 }}>{restaurant.hero.description}</p><div className="flex flex-col sm:flex-row gap-4 justify-center animate-fade-up" style={{ animationDelay: '0.2s', opacity: 0 }}><Button onClick={() => scroll('menu')} className="group hover:shadow-xl flex items-center justify-center gap-2">{restaurant.hero.primaryCTA}<ArrowRight size={16} className="group-hover:translate-x-1 transition-transform" /></Button><Button variant="secondary" onClick={() => scroll('contact')}>{restaurant.hero.secondaryCTA}</Button></div></div><button onClick={() => scroll('menu')} className="absolute bottom-10 left-1/2 -translate-x-1/2 text-charcoal-400 hover:text-white transition-colors flex flex-col items-center gap-1 animate-fade-in" style={{ animationDelay: '0.8s', opacity: 0 }} aria-label="Scroll to menu"><span className="text-xs tracking-widest uppercase">Scroll</span><ChevronDown size={18} className="animate-bounce" /></button><div className="absolute bottom-0 inset-x-0 z-10"><div className="max-w-5xl mx-auto px-6 pb-10 hidden lg:flex gap-12 justify-center">{restaurant.hero.stats.map((stat) => <div key={stat.label} className="text-center"><p className="font-display text-2xl font-bold text-white">{stat.value}</p><p className="text-charcoal-400 text-xs tracking-wide">{stat.label}</p></div>)}</div></div></section>; }
