import { ArrowUpRight } from 'lucide-react';
import { homepageCategories } from '../../data/menu';
import { handleInternalNavigation } from '../../hooks/useRoute';
import { FallbackImage } from '../ui/FallbackImage';

const teaserIds = ['burgers', 'chicken', 'wraps-naans', 'specialites', 'plats'];
const teaserCategories = teaserIds.map(id => homepageCategories.find(category => category.id === id)!);
export function CategoryTeaser() { return <section className="bg-background py-20 sm:py-24 overflow-hidden"><div className="max-w-7xl mx-auto px-5"><p className="eyebrow">Toutes les envies</p><div className="flex items-end justify-between gap-6"><h2 className="editorial-title max-w-4xl">CHOISISSEZ VOTRE<br/><span className="text-accent">PROCHAINE OBSESSION.</span></h2><ArrowUpRight className="hidden sm:block text-accent" size={42}/></div><div className="mt-12 grid grid-cols-2 md:grid-cols-5 gap-6 sm:gap-8">{teaserCategories.map(category => <a key={category.id} href={`/menu?category=${category.id}`} onClick={event => handleInternalNavigation(event, `/menu?category=${category.id}`)} className="group text-center"><span className="relative block aspect-square overflow-hidden rounded-full border-4 border-white shadow-[0_14px_35px_rgba(59,42,33,.12)]"><FallbackImage src={category.image} fallbackSrc={category.fallbackImage} alt={category.name} loading="lazy" className="h-full w-full object-cover transition-transform duration-500 group-hover:scale-110"/></span><strong className="mt-4 block font-display text-lg font-black text-heading group-hover:text-accent">{category.name}</strong></a>)}</div></div></section>; }
