import { ArrowUpRight } from 'lucide-react';
import { homepageCategories } from '../../data/menu';
import { handleInternalNavigation } from '../../hooks/useRoute';
import { FallbackImage } from '../ui/FallbackImage';

const teaserIds = ['burgers', 'chicken', 'wraps-naans', 'specialites', 'plats'];
const teaserCategories = teaserIds.map(id => homepageCategories.find(category => category.id === id)!);
export function CategoryTeaser() { return <section className="bg-background-alt py-20 sm:py-28 overflow-hidden"><div className="max-w-7xl mx-auto px-5"><p className="eyebrow">Toutes les envies</p><h2 className="editorial-title max-w-4xl">CHOISISSEZ VOTRE<br/><span className="text-accent">PROCHAINE OBSESSION.</span></h2></div><div className="mt-10 flex gap-4 overflow-x-auto px-[max(1.25rem,calc((100vw-80rem)/2))] pb-5 snap-x menu-tabs">{teaserCategories.map((category,index) => <a key={category.id} href={`/menu?category=${category.id}`} onClick={event => handleInternalNavigation(event, `/menu?category=${category.id}`)} className={`group relative shrink-0 w-[72vw] sm:w-[42vw] lg:w-[24rem] overflow-hidden rounded-[2rem] snap-start ${index % 2 ? 'h-[360px]' : 'h-[430px]'}`}><FallbackImage src={category.image} fallbackSrc={category.fallbackImage} alt={category.name} loading="lazy" className="tile-image"/><span className="tile-overlay"/><span className="absolute inset-x-0 bottom-0 p-6 flex items-end justify-between text-white"><strong className="font-display text-2xl font-black">{category.name}</strong><span className="w-10 h-10 rounded-full bg-primary text-heading flex items-center justify-center"><ArrowUpRight/></span></span></a>)}</div></section>; }
