import { restaurant } from '../../config/restaurant';
export function Footer() {
  const socials = Object.entries(restaurant.social).filter(([,url]) => url);
  return <footer className="bg-footer text-footer py-14 px-5"><div className="max-w-7xl mx-auto grid sm:grid-cols-2 lg:grid-cols-4 gap-9"><div><p className="font-display text-2xl font-black">{restaurant.shortName}</p><p className="text-primary uppercase text-xs font-bold tracking-[.25em] mt-2">{restaurant.footer.tagline}</p></div><div><p className="footer-heading">Adresse</p><p>{restaurant.address}<br/>{restaurant.postcode} {restaurant.city}</p></div><div><p className="footer-heading">Contact</p><a href={`tel:${restaurant.phone}`} className="hover:text-primary">{restaurant.phoneDisplay}</a><p className="mt-2">{restaurant.openingHours[0].label}<br/>{restaurant.openingHours[0].value}</p></div><div><p className="footer-heading">Navigation</p><nav className="flex flex-wrap gap-x-4 gap-y-2">{restaurant.navigation.map(link => <button key={link.target} onClick={() => document.getElementById(link.target)?.scrollIntoView({behavior:'smooth'})} className="hover:text-primary text-sm">{link.label}</button>)}</nav>{socials.length > 0 && <div className="mt-4 flex gap-3">{socials.map(([label,url]) => <a key={label} href={url} className="capitalize hover:text-primary">{label}</a>)}</div>}</div></div><div className="max-w-7xl mx-auto mt-10 pt-6 border-t border-footer/20 text-xs opacity-70">© {new Date().getFullYear()} {restaurant.businessName}. {restaurant.footer.rightsText}</div></footer>;
}
