import { restaurant } from '../../config/restaurant';
export function Footer() { return <footer className="bg-footer border-t border-charcoal-800 py-12 px-6"><div className="max-w-7xl mx-auto flex flex-col md:flex-row items-center justify-between gap-6"><div><p className="font-display text-2xl font-bold text-white">{restaurant.shortName}<span className="text-brand-500">.</span></p><p className="text-charcoal-500 text-xs mt-1">{restaurant.footer.tagline}</p></div><nav className="flex flex-wrap gap-6 justify-center" aria-label="Footer navigation">{restaurant.navigation.map((link) => <button key={link} onClick={() => document.getElementById(link.toLowerCase())?.scrollIntoView({ behavior: 'smooth' })} className="text-charcoal-400 hover:text-white text-sm transition-colors">{link}</button>)}</nav><p className="text-charcoal-600 text-xs text-center md:text-right">&copy; {new Date().getFullYear()} {restaurant.footer.businessText}<br />{restaurant.footer.rightsText}</p></div></footer>; }
