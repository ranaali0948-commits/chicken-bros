import { restaurant } from '../../config/restaurant';
import { handleInternalNavigation } from '../../hooks/useRoute';
export function Footer() {
  const socials=Object.entries(restaurant.social).filter(([,url])=>url); const delivery=[{label:restaurant.order.uberEatsLabel,url:restaurant.ordering.uberEatsUrl},{label:restaurant.order.deliverooLabel,url:restaurant.ordering.deliverooUrl}].filter(link=>link.url);
  return <footer className="bg-footer text-footer-text py-12 px-5"><div className="max-w-7xl mx-auto grid sm:grid-cols-2 lg:grid-cols-4 gap-9"><div><p className="font-display text-2xl font-black">{restaurant.businessName}</p><p className="text-primary uppercase text-xs font-bold tracking-[.25em] mt-2">{restaurant.footer.tagline}</p></div><div><p className="footer-heading">Adresse</p><p>{restaurant.address}<br/>{restaurant.postcode} {restaurant.city}</p></div><div><p className="footer-heading">Contact</p><a href={`tel:${restaurant.phone}`} className="hover:text-primary">{restaurant.phoneDisplay}</a><p className="mt-2">{restaurant.openingHours[0].label}<br/>{restaurant.openingHours[0].value}</p></div><div><p className="footer-heading">Navigation</p><nav className="flex flex-col items-start gap-2">{restaurant.navigation.map(link=><a key={link.href} href={link.href} onClick={event=>handleInternalNavigation(event,link.href)} className="text-sm hover:text-primary">{link.label}</a>)}</nav></div></div><div className="max-w-7xl mx-auto mt-8 pt-6 border-t border-white/15 flex flex-wrap gap-3">{delivery.map(link=><a key={link.label} href={link.url} target="_blank" rel="noopener noreferrer" className="footer-pill">{link.label}</a>)}{socials.map(([label,url])=><a key={label} href={url} target="_blank" rel="noopener noreferrer" className="footer-pill capitalize">{label}</a>)}</div><div className="max-w-7xl mx-auto mt-7 text-xs text-footer-text/70">© {new Date().getFullYear()} {restaurant.businessName}. {restaurant.footer.rightsText}</div></footer>;
}
